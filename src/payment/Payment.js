import React from 'react';
import axios from 'axios';

class Payment extends React.Component {
    handlePayment = (category) => {
        // Handle payment logic for categories
    };

    render() {
        return <div>Payment Screen</div>;
    }
}

export default Payment;